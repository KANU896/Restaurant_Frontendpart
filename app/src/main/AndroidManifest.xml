<?xml version="1.0" encoding="utf-8"?>


<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION">
    </uses-permission>

    <application
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        ...>
        <activity android:name=".User_Map_Page"
            tools:ignore="MissingClass,WrongManifestParent">
            ...
        </activity>
        <meta-data android:name="com.kakao.sdk.AppKey" android:value="8a6b68429b3a1797f3f9f878beb26f40"/>
    </application>



    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".User_MainScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".User_Login"/>
        <activity android:name=".User_Register"/>
        <activity android:name=".User_IdFind"/>
        <activity android:name=".User_Main"/>
        <activity android:name=".User_Home_Page"
            tools:ignore="Instantiatable" />  //버튼 누르면 화면전환 (민우)
        <activity android:name=".User_Myfavorite_Page2"/>
    </application>

    </manifest>